version: '3.8'

services:
  app:
    build: .
    container_name: raft-labs-app
    ports:
      - '4000:4000'
    environment:
      - MONGO_URI=mongodb://mongo:27017/raft-labs-db
      - JWT_SECRET=your_jwt_secret
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    container_name: raft-labs-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: